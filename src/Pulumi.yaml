name: pulumi_orchestrater
description: Full bootstrap orchestrate your infrastructure though a single yaml
main: pipeline_orchestrator/
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
